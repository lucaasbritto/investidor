services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: laravel_app_investidor
    volumes:
      - ./backend:/var/www/html
    ports:
      - "9000:9000"
    depends_on:
      - db
    networks:
      - investidor-network
    environment:
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: investidor
      DB_USERNAME: laravel
      DB_PASSWORD: laravel

  webserver:
    image: nginx:alpine
    container_name: laravel_web_investidor
    ports:
      - "8080:80"
    volumes:
      - ./backend:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend
    networks:
      - investidor-network

  db:
    image: mysql:8.0
    container_name: laravel_db_investidor
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: laravel
      MYSQL_PASSWORD: laravel
    ports:
      - "3307:3306"
    volumes:
      - dbdata:/var/lib/mysql
      - ./_docker/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - investidor-network

  frontend:
    build:
      context: ./frontend
    container_name: vue_frontend_investidor
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    networks:
      - investidor-network

volumes:
  dbdata:

networks:
  investidor-network:
    driver: bridge